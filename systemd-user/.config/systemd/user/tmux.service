[Unit]
Description=tmux session for user '%u'
After=graphical-session.target

[Service]
Type=simple
Restart=always
ExecStart=/usr/bin/tmux new-session -s %u
ExecStop=/usr/bin/tmux kill-session -t %u

[Install]
WantedBy=default.target
